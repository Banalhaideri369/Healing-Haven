========================================================
  BAN AL-HAIDARI — ENVIRONMENT VARIABLES GUIDE
  Copy-paste these blocks into your hosting dashboards.
========================================================


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  BLOCK 1: NETLIFY (Frontend)
  Dashboard → Site Settings → Environment Variables
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

VITE_API_BASE_URL=https://your-render-app.onrender.com

VITE_FIREBASE_API_KEY=your_firebase_api_key
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  BLOCK 2: RENDER (Backend / API Server)
  Dashboard → Your Service → Environment
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

PORT=5000
NODE_ENV=production

DATABASE_URL=postgresql://user:password@host.neon.tech/neondb?sslmode=require

STRIPE_SECRET_KEY=sk_live_your_stripe_secret_key

CORS_ORIGIN=https://your-site.netlify.app

FIREBASE_PROJECT_ID=your-project-id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxx@your-project.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

VAPID_PUBLIC_KEY=your_vapid_public_key
VAPID_PRIVATE_KEY=your_vapid_private_key


========================================================
  NOTES
  - Replace every placeholder with your actual values.
  - VITE_API_BASE_URL must be the full Render URL with
    NO trailing slash (e.g. https://ban-api.onrender.com)
  - CORS_ORIGIN must be the full Netlify URL with
    NO trailing slash (e.g. https://ban-site.netlify.app)
  - DATABASE_URL from Neon already includes sslmode=require
  - FIREBASE_PRIVATE_KEY must keep the \n characters and
    be wrapped in double quotes on Render.
========================================================
